---
name: Continuous Integration

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - "3.10"
          - "3.14"
    steps:
      - name: Checking out code from GitHub
        uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: pip-${{ hashFiles('**/Pipfile.lock') }}-${{ matrix.python-version }}
          restore-keys: |
            pip-

      - name: Install dependencies
        run: |
          pip install pipenv
          pipenv sync --dev

      - name: Run linters
        run: pipenv run make lint

      - name: Run prek
        if: ${{ matrix.python-version }} == "3.14"
        run: |
          pipenv run prek run \
            end-of-file-fixer \
            trailing-whitespace \
            check-yaml \
            check-json \
            check-toml \
            mixed-line-ending \
            --all-files

      - name: Run tests
        run: pipenv run make test

  test:
    runs-on: ubuntu-latest
    needs: [lint]
    if: always()
    steps:
      - name: Check lint results
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ]]; then
            echo "Lint failed"
            exit 1
          fi
